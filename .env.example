# Copie este arquivo para .env e preencha com suas chaves.
# O arquivo .env fica na mesma pasta que o pubspec.yaml (raiz do projeto).
# Nunca commite o .env (ele já está no .gitignore).
#
# Para build no GitHub Actions: crie um secret chamado ENV_FILE_CONTENT
# com o conteúdo completo deste arquivo (com as chaves preenchidas).
# O workflow cria o .env a partir desse secret antes do build.

# Supabase (licenças, ranking global, amigos, perfil)
# Obtenha em: Supabase Dashboard do SEU projeto → Settings → API
# IMPORTANTE: SUPABASE_URL tem de ser do MESMO projeto onde executou as migrações
# (ex.: se o ranking no SQL Editor devolve dados mas no app está vazio = URL diferente)
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_ANON_KEY=sua_chave_anon_aqui

# TMDB (filmes/séries - sinopse, elenco, imagens)
# Obtenha em: https://www.themoviedb.org/settings/api
TMDB_API_KEY=sua_chave_tmdb_aqui

# Stripe (pagamentos)
# Obtenha em: https://dashboard.stripe.com/apikeys
STRIPE_PUBLISHABLE_KEY=pk_live_sua_chave_aqui
STRIPE_PRICE_ID=price_seu_id_aqui
STRIPE_PRODUCT_ID=prod_seu_id_aqui
